<% include partials/head.ejs %>
    <title>Matches - Drinkerz</title>
</head>
<body>
    <header>
        <nav>
            <ul>
                <li><a href="/"><img src="/static/media/images/cyclove-logo.svg" alt="Logo CycLove"></a></li>
                <li><a href="/log-out">Logout</a></li>
            </ul>
        </nav>
    </header>
    <h1>Your matches</h1>
    <main>
        <section class="card-grid">
                <!-- Here will be the matches shown based on the user -->
                <% for (let i = 0; i < likedpersons.length; i++) { %>
                <a href="/<%= likedpersons[i].id %>">
                    <article class="card">
                        <!-- Image of the user -->
                        <img src="<%= likedpersons[i].image %>" alt="Profile picture from <%= likedpersons[i].firstName %>" class="card__img">
                        <!-- Send a like to the array and saves it -->
                        <form action="/matches" method="GET">
                            <input type="hidden" name="currentPerson" value="<%= likedpersons[i].id %>">
                            <button type="submit">Dislike <%= likedpersons[i].firstName %></button>
                        </form>
                        <!-- Name and city the person lives in -->
                        <section>
                            <h2><%= likedpersons[i].firstName + " " + likedpersons[i].lastName %></h2>
                            <p><%= likedpersons[i].city %></p>
                        </section>
                    </article>
                </a>
            <% } %>
        </section>
    </main>
    <% include partials/footer.ejs %>
</body>
</html>